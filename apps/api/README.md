# api

Scaffold reserved for future online multiplayer. The game is fully client-side today;
this app currently exposes only a `GET /health` endpoint.

```bash
pnpm --filter api start:dev   # http://localhost:4000/health
```
